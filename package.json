{
  "name": "foraxx-palette-photoshop",
  "version": "0.1.0",
  "private": true,
  "description": "Photoshop UXP plugin that builds a Foraxx palette as screened, editable layers",
  "scripts": {
    "test": "node --test",
    "package": "node scripts/package.cjs"
  }
}
